class Workshop {
  String title;
  DateTime timing;
  String facilitator;
  DateTime dueDate;
  String description;

  Workshop({
    required this.title,
    required this.timing,
    required this.facilitator,
    required this.dueDate,
    required this.description,
  });
}
